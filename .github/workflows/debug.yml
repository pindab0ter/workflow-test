name: Debug

on:
  push:
    branches:
      - main
      - develop
  workflow_dispatch: {}

jobs:
  debug:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Run script
        run: /bin/bash ./deploy.sh
